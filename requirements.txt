# hermes-bridge 어댑터 런타임 의존성 (Python 3.11+)
mcp>=1.0
httpx>=0.27
# send 도구는 Hermes 의 tools.send_message_tool 을 import 한다 (HERMES_AGENT_PATH 로 경로 지정).
# P2 패키징: PyInstaller/zipapp 으로 단일 실행파일 + SHA256 체크섬.
